@model CidicomPrototype.Models.Activos

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Activos</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.nameActivos)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.nameActivos)
            @Html.ValidationMessageFor(model => model.nameActivos)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.IDSitios, "Sitios")
        </div>
        <div class="editor-field">
            @Html.DropDownList("IDSitios", String.Empty)
            @Html.ValidationMessageFor(model => model.IDSitios)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
