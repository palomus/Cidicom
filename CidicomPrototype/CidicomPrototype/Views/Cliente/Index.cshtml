@model IEnumerable<CidicomPrototype.Models.Clientes>

@{
    ViewBag.Title = "Clientes";
    ViewBag.viewMessage = "Clientes";
}

<h2>Clientes</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NameCliente)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NameCliente)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IDCliente }) |
            @Html.ActionLink("Details", "Details", new { id=item.IDCliente }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IDCliente })|
            @Html.ActionLink("sus Sitios","Index","Sitios", new { id = item.IDCliente },null)
        </td>
    </tr>
}

</table>
