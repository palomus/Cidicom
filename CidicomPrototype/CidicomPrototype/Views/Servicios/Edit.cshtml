@model CidicomPrototype.Models.Servicios

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Servicios</legend>

        @Html.HiddenFor(model => model.IDServicios)

        <div class="editor-label">
            @Html.LabelFor(model => model.nameServicios)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.nameServicios)
            @Html.ValidationMessageFor(model => model.nameServicios)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.IDActivos, "Activos")
        </div>
        <div class="editor-field">
            @Html.DropDownList("IDActivos", String.Empty)
            @Html.ValidationMessageFor(model => model.IDActivos)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
